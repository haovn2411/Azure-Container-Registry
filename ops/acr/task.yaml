version: v1.1.0

values:
  - name: repo
    default: thesportcheck-dev
  - name: keep
    default: 500
  - name: ago
    default: 0d

steps:
  - cmd: >
      sh ops/acr/preview.sh {{.Values.repo}} {{.Values.keep}} {{.Values.ago}}
    image: mcr.microsoft.com/azure-cli
    disableWorkingDirectoryOverride: true
    timeout: 3600
