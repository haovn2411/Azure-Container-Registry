version: v1.1.0

values:
  - name: repo
    default: thesportcheck-dev
  - name: keep
    default: 500
  - name: ago
    default: 0d

steps:
  - cmd: |
      echo "Start purging repo: {{.Values.repo}}"
    timeout: 3600
